query Hosts {
  hosts {
    name
    lastBackup {
      number
      startDate
      fileSize
      completed
      agentVersion
    }
    agentVersion
    availibilityState
    timeSinceLastBackup
    dateToNextBackup
    lastBackupState
    configuration {
      schedule {
        activated
      }
    }
  }
}
