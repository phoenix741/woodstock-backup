query Hosts {
  hosts {
    name
    lastBackup {
      number
      startDate
      fileSize
      status
      agentVersion
    }
    agentVersion
    availibilityState
    timeSinceLastBackup
    dateToNextBackup
    configuration {
      schedule {
        activated
      }
    }
  }
}
