query Hosts {
  hosts {
    name
    lastBackup {
      number
      startDate
      fileSize
      completed
      agentVersion
    }
    agentVersion
    timeSinceLastBackup
    dateToNextBackup
    lastBackupState
    configuration {
      schedule {
        activated
      }
    }
  }
}
