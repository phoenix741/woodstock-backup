query Hosts {
  hosts {
    name
    lastBackup {
      number
      startDate
      fileSize
      completed
    }
    timeSinceLastBackup
    dateToNextBackup
    lastBackupState
    configuration {
      schedule {
        activated
      }
    }
  }
}
