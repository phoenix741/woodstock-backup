query Hosts {
  hosts {
    name
    lastBackup {
      number
      startDate
      fileSize
      completed
    }
    lastBackupState
    configuration {
      schedule {
        activated
      }
    }
  }
}
