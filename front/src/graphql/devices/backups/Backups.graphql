query Backups($hostname: String!) {
  backups(hostname: $hostname) {
    number
    completed
    startDate
    endDate
    fileCount
    newFileCount
    existingFileCount
    fileSize
    newFileSize
    existingFileSize
    speed
  }
}
